# This workflow will run the tests on every push and pull request to the repository.

name: Tests

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]  # TODO: Add more versions
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v2
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pytest pytest-cov

    - name: Install the base package
      run: |
        pip install .

    - name: Install the backends
      run: |
        pip install jax

    - name: Run the tests
      run: |
        export FRIDOM_BACKEND="numpy"
        pytest --cov  --cov-report=xml --junitxml=junit_numpy.xml -o junit_family=legacy .
        export FRIDOM_BACKEND="jax_cpu"
        pytest --cov  --cov-report=xml --junitxml=junit_jax.xml -o junit_family=legacy .

    - name: Upload numpy test results
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: junit_numpy.xml
        flags: numpy

    - name: Upload jax_cpu test results
      if: ${{ !cancelled() }}
      uses: codecov/test-results-action@v1
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
        files: junit_jax.xml
        flags: jax_cpu

    - name: Upload coverage reports to Codecov
      uses: codecov/codecov-action@v5
      with:
        token: ${{ secrets.CODECOV_TOKEN }}
