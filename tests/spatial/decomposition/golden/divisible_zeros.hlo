HloModule <m> is_scheduled=true, entry_computation_layout={()->f64[24]{0}}, allow_spmd_sharding_propagation_to_output={true}, num_partitions=4
%wrapped_broadcast_computation (param_0: f64[]) -> f64[24] {
  %param_0 = f64[] parameter(0)
  ROOT %broadcast.3 = f64[24]{0} broadcast(%param_0), dimensions={}, metadata={op_name="broadcast.1"}
}

ENTRY %main.0_spmd () -> f64[24] {
  %constant.0.clone = f64[] constant(0)
  ROOT %wrapped_broadcast = f64[24]{0} fusion(%constant.0.clone), kind=kLoop, calls=%wrapped_broadcast_computation, metadata={op_name="broadcast.1"}
}
